describe('Add User Component', () => {

  let sut: { a?: any; };

  beforeEach(() => {
    sut = {}
  })

  it('should be true', () => {
    sut.a = true;

    // arrange, act, assert
    expect(sut.a).toBe(true);
  })
})
